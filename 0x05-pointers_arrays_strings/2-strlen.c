#include "main.h"
/**
 * strlen - function that returns the length of a string.
 * @s: pointer variable
 * return: integer value
 */
int _strlen(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; x++)
	{
	}
	return (x);

}
